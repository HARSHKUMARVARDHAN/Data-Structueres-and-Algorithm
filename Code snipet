##CONVERT ASCII VALUE TO ENGLISH LETTER ## char c = static_cast<char>( ascii value );
# include <cmath>  .... ceil(2.2) GIVES 3 AND round(2.2) GIVES 2
# CANT ACCESS ELEMENTS OF MAP USING INDEX , for(auto &pair : mp){vec.push_back(pair.first);}
# ACCESS LAST BIT OF INTEGER .... n = n&1
# Convert char to string .... string(1, s[i]));
# remove part of string ..... str.erase(index , length)
# remove particular index element from vector ........ vec.erase(vec.begin() + index);
# insert new vector from index2 of vec2 at particular index1 in vec.....vec.insert(vec.begin()+index1,vec2.begin()+index2 , vec2.end()) 
# count number of set bits ........int setBitCount = __builtin_popcount(nums[i]);
# want only last n bits then masking .......int mask = (1<<bitNeeded) -1; (num & mask);
